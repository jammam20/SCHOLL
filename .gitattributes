# Normalize line endings on checkout/commit so the repo behaves the same
# whether you're on Windows, macOS, or Linux.
* text=auto eol=lf

# Binary assets — never text-normalize these.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.jks binary
*.keystore binary
