name: school_shared
description: Shared domain contracts, models and constants for the School Bus System.
publish_to: none
environment:
  sdk: ^3.13.0
resolution: workspace
dependencies:
  equatable: ^2.0.7
  meta: ^1.18.0
dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.15
